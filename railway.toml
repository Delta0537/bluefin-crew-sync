# Docker build: pins Node 22 and avoids Nixpacks detection quirks.
# https://docs.railway.com/guides/dockerfiles

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node .output/server/index.mjs"
healthcheckPath = "/"
healthcheckTimeout = 120
